# Repo-Local Connector State

This directory is the optional repo-local state root for small or starter
connector databases.

The directory exists in Git only as a scaffold. Generated content under it is
ignored and must not be committed.

Use it when a fresh clone has enough local disk and no external storage has
been configured yet. Move to external storage when a connector run grows beyond
small bounded experiments.

